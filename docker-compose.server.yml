services:
  traefik:
    image: traefik:v3.6
    profiles:
      - edge
    environment:
      DOCKER_API_VERSION: "1.41"
    command:
      - "--api.insecure=false"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.web.http.redirections.entryPoint.to=websecure"
      - "--entrypoints.web.http.redirections.entryPoint.scheme=https"
      - "--certificatesresolvers.myresolver.acme.httpchallenge=true"
      - "--certificatesresolvers.myresolver.acme.httpchallenge.entrypoint=web"
      - "--certificatesresolvers.myresolver.acme.email=${TRAEFIK_ACME_EMAIL:-admin@example.com}"
      - "--certificatesresolvers.myresolver.acme.storage=/letsencrypt/acme.json"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - letsencrypt:/letsencrypt
    networks:
      - proxy_network

  backend:
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${TRAEFIK_ROUTER_PREFIX:-santagym}-backend.rule=Host(`${API_HOST:-api.gym.example.com}`)"
      - "traefik.http.routers.${TRAEFIK_ROUTER_PREFIX:-santagym}-backend.entrypoints=websecure"
      - "traefik.http.routers.${TRAEFIK_ROUTER_PREFIX:-santagym}-backend.tls.certresolver=myresolver"
      - "traefik.http.services.${TRAEFIK_ROUTER_PREFIX:-santagym}-backend.loadbalancer.server.port=8000"
      - "traefik.docker.network=proxy_network"

  bot:
    restart: unless-stopped

  frontend:
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${TRAEFIK_ROUTER_PREFIX:-santagym}-frontend.rule=Host(`${FRONTEND_HOST:-gym.example.com}`)"
      - "traefik.http.routers.${TRAEFIK_ROUTER_PREFIX:-santagym}-frontend.entrypoints=websecure"
      - "traefik.http.routers.${TRAEFIK_ROUTER_PREFIX:-santagym}-frontend.tls.certresolver=myresolver"
      - "traefik.http.services.${TRAEFIK_ROUTER_PREFIX:-santagym}-frontend.loadbalancer.server.port=80"
